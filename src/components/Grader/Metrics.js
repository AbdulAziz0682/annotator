import { Grid } from "@material-ui/core";

export default function Metrics(props){
    return (
        <Grid container>
            <Grid item>
                Metrics
            </Grid>
        </Grid>
    )
}