import { Typography } from '@material-ui/core';
import React from 'react';

export default function Home(props){
    return (
        <Typography variant="h3" color="primary" align="center">Home</Typography>
    )
}